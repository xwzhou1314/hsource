<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hsource.item.mapper.TbCategoryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hsource.item.entity.TbCategory">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="parent_id" property="parentId" />
        <result column="is_parent" property="isParent" />
        <result column="sort" property="sort" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id AS id, name AS name, parent_id AS parentId, is_parent AS isParent, sort AS sort
    </sql>

    <select id="idList" resultType="com.hsource.item.entity.TbCategory">
        SELECT ID
        FROM tb_category
    </select>

</mapper>
